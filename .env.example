# Environment variables for VC-INGRESO (backend PHP)
# Copy this file to .env and adjust values for your environment.

APP_ENV=local
APP_DEBUG=true

# Database connection
DB_HOST=localhost
DB_PORT=3306
DB_NAME=vc_entrance
DB_USER=root
DB_PASS=change_me
DB_CHARSET=utf8mb4

# CORS
CORS_ALLOW_ORIGIN=*
